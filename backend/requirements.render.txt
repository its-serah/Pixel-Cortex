# Minimal dependencies for Render deployment (no Torch/Whisper/Transformers/Spacy)
# Keeps image small (<500MB) and supports core API (auth/users/tickets/policies/triage)

fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
httpx==0.27.2
python-dotenv==1.0.0
email-validator==2.1.0.post1

# DB
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Retrieval (BM25 + TF‑IDF) used by policy retriever / triage planner
rank-bm25==0.2.2
nltk==3.8.1
numpy==1.25.2
PyPDF2==3.0.1

# CPU-only Speech-to-Text (optional; disabled in minimal Render build)
# vosk==0.3.45
# soundfile==0.12.1

# Lightweight LLM Options (choose ONE):

# Option 1: Use external API (Together.ai, OpenAI, etc)
requests==2.31.0

# Option 2: ONNX Runtime for tiny models (RECOMMENDED)
onnxruntime==1.16.0  # Only 15MB!

# Option 3: Connect to external Ollama server
ollama==0.3.3

# Option 4: TinyLlama via llama-cpp (CPU only, ~200MB)
# llama-cpp-python==0.2.20  # TEMPORARILY DISABLED - uncomment after fixing other issues
